import { createTheme, Theme } from "@mui/material/styles";

export const themePalette: Theme = createTheme({
  palette: {
    primary: {
      main: "#241623",
    },
    secondary: {
      main: "#94efe0c2",
    },
    info: { main: "#ffff" },
  },
  spacing: 4,
});
